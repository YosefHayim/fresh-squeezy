# Lemon Squeezy API key. Get one at https://app.lemonsqueezy.com/settings/api
LEMON_SQUEEZY_API_KEY=

# Either "test" or "live". Defaults to "test" if unset.
LEMON_SQUEEZY_MODE=test

# Optional: set to 1 to enable opt-in live smoke tests (CI nightly only).
# LEMON_SQUEEZY_LIVE_SMOKE=1
